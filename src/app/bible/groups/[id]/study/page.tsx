'use client';

import { EmptyState } from '../../components/EmptyState';

export default function StudyPage() {
  return (
    <EmptyState
      title="Group reading plans land in v1.1"
      body="The schema is ready (curated and AI-assisted plans). Per-day discussion threads and shared verse annotations land next."
    />
  );
}
