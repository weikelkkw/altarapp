'use client';

import { EmptyState } from '../../components/EmptyState';

export default function MeetingsPage() {
  return (
    <EmptyState
      title="Meetings & RSVPs land in v1.1"
      body="Schema and RSVP attendance are ready. Agenda templates, calendar integration, and live mode follow."
    />
  );
}
